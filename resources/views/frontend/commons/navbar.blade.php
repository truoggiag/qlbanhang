<!-- NAVIGATION -->
<nav id="navigation">
    <!-- container -->
    <div class="container">
        <!-- responsive-nav -->
        <div id="responsive-nav">
            <!-- NAV -->
            <ul class="main-nav nav navbar-nav">
        {{--          @foreach($data['brands'] as $key => $item)
                    <li class="{{$item->slug === $data['slugBrand'] ? 'active' : ''}}">
                        <a href="{{route('fr.brand',['slug' => $item->slug])}}">{{$item->name}}</a>
                    </li>
                @endforeach  --}}
            </ul>
            <!-- /NAV -->
        </div>
        <!-- /responsive-nav -->
    </div>
    <!-- /container -->
</nav>
<!-- /NAVIGATION -->
